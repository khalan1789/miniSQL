{
  "name": "mini-sql",
  "version": "1.0.0",
  "description": "a mini sql application",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "mkdir users && tsc --build",
    "start": " node ./dist/index.js "
  },
  "author": "Benjamin Ducau",
  "license": "ISC",
  "devDependencies": {
       "typescript": "^5.0.4"
  },
  "dependencies": {
     "@types/node": "^18.15.12"
  }
}
